Такой подход идеален для новичков — шаг за шагом, без перегрузки.

markdown
# 📚 Урок 1: Создаём простую 2D-игру на Pygame

**Цель урока:**  
👉 Постепенно создать игру с игроком, который может двигаться и прыгать  
👉 Разобраться с основными концепциями: цикл, события, классы, физика

---

## 1️⃣ Подключаем библиотеку Pygame

Сначала установи Pygame (если ещё не установлен):

```bash
pip install pygame
```

Теперь создай файл main.py и добавь в него импорт:

```python
import pygame
import sys
```
Инициализируем Pygame — запускаем все его подсистемы:

```python
pygame.init()
```
💡 pygame.init() — обязательно в начале любой игры на Pygame

2️⃣ Создаём класс игрока
Теперь создадим класс Player, который будет управлять нашим персонажем.

```python
class Player:
    def __init__(self, x, y):
        self.x = x                    # Позиция по горизонтали
        self.y = y                    # Позиция по вертикали
        self.width = 50               # Ширина игрока
        self.height = 50              # Высота игрока
        self.vel_y = 0                # Скорость по вертикали (для прыжка)
        self.is_jumping = False       # Состояние: прыгает или нет
        self.rect = pygame.Rect(x, y, self.width, self.height)
```
✅ Мы используем pygame.Rect — удобный объект для отрисовки и проверки столкновений
✅ Игрок создаётся с начальными координатами (x, y)

3️⃣ Основной игровой цикл и выход
Теперь создадим окно и запустим главный цикл игры.

```python
# Настройки окна
SCREEN_WIDTH = 800
SCREEN_HEIGHT = 600
screen = pygame.display.set_mode((SCREEN_WIDTH, SCREEN_HEIGHT))
pygame.display.set_caption("Игра: Перемещение и прыжок")

# Цвета
WHITE = (255, 255, 255)

# Создаём игрока
player = Player(100, 450)  # Стартовая позиция

# Основной цикл игры
clock = pygame.time.Clock()
running = True

while running:
    clock.tick(60)  # 60 кадров в секунду
    screen.fill(WHITE)

    # Обработка событий
    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            running = False

    # Сюда будем добавлять управление и физику

    # Обновление экрана
    pygame.display.flip()

# Завершение программы
pygame.quit()
sys.exit()
```
✅ clock.tick(60) — стабилизирует частоту кадров
✅ event.get() — проверяет действия игрока (например, закрытие окна)
✅ flip() — отображает всё, что нарисовано за кадр

4️⃣ Добавляем метод: движение влево и вправо
Добавим в класс Player метод move():

```python
    def move(self, direction):
        if direction == "left":
            self.x -= 15
        elif direction == "right":
            self.x += 15

        # Ограничиваем движение по краям экрана
        if self.x < 0:
            self.x = 0
        if self.x > 800 - self.width:
            self.x = 800 - self.width
```
Теперь добавим управление в основной цикл. Замени комментарий на:

```python
    # Управление (удержание клавиш)
    keys = pygame.key.get_pressed()
    if keys[pygame.K_a]:
        player.move("left")
    if keys[pygame.K_d]:
        player.move("right")
```
✅ get_pressed() — проверяет, зажата ли клавиша
✅ Управление: A — влево, D — вправо

5️⃣ Добавляем метод: прыжок
Добавим метод jump() в класс Player:

```python
    def jump(self):
        if not self.is_jumping:  # Прыгаем только если на земле
            self.vel_y = -20     # Отрицательная скорость = движение вверх
            self.is_jumping = True
```
Добавим вызов в основной цикл:

```python
    if keys[pygame.K_SPACE]:
        player.jump()
```
6️⃣ Добавляем метод: гравитация и приземление
Теперь добавим apply_gravity() — чтобы игрок падал обратно на землю.

```python
    def apply_gravity(self):
        self.vel_y += 1          # Гравитация: увеличиваем скорость вниз
        self.y += self.vel_y     # Двигаем игрока

        # Линия земли: y = 500 (600 - 100)
        ground_y = 500
        if self.y >= ground_y - self.height:
            self.y = ground_y - self.height
            self.vel_y = 0
            self.is_jumping = False

        # Обновляем rect, чтобы отрисовка была точной
        self.rect.topleft = (self.x, self.y)
```
Добавь этот метод в цикл:

```python
    player.apply_gravity()
```
7️⃣ Рисуем игрока на экране
Добавим метод draw():

```python
    def draw(self, surface):
        pygame.draw.rect(surface, (0, 0, 255), self.rect)  # Синий квадрат
```
И вызов в цикле:

```python
    player.draw(screen)
```
8️⃣ Рисуем землю (визуальная опора)
Чтобы было понятно, где "земля", добавим линию:

```python
    # Рисуем землю
    pygame.draw.line(screen, (255, 0, 0), (0, 500), (800, 500), 3)
```
✅ Красная линия на высоте y = 500 — визуальная опора

🏁 Готово!
Теперь твой полный main.py работает!
Игрок:

Двигается стрелками (A, D)
Прыгает (Пробел)
Не проваливается сквозь землю
Не выходит за края экрана
