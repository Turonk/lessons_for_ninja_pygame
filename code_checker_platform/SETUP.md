# Инструкция по установке и запуску

## 📋 Требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd code_checker_platform
pip install -r requirements.txt
```

### 2. Запуск backend сервера

```bash
cd backend
python app.py
```

Сервер запустится на `http://localhost:5000`

### 3. Открытие frontend

Открой файл `frontend/index.html` в браузере (можно просто дважды кликнуть на файл).

Или используй простой HTTP сервер:

```bash
cd frontend
python -m http.server 8000
```

Затем открой в браузере: `http://localhost:8000`

## 🧪 Тестирование

### Проверка API

```bash
# Проверка работоспособности
curl http://localhost:5000/api/health

# Получение задания
curl http://localhost:5000/api/exercise/lesson_03a/6

# Проверка кода (пример)
curl -X POST http://localhost:5000/api/check \
  -H "Content-Type: application/json" \
  -d '{
    "code": "age = 12\nname = \"Игрок\"\nprint(f\"Имя: {name}\")",
    "lesson": "lesson_03a",
    "exercise": 6
  }'
```

## 📝 Добавление новых заданий

1. Создай JSON файл в `exercises/lesson_XX/exercise_N.json`
2. Используй формат из примеров `exercise_6.json` и `exercise_7.json`
3. Перезапусти backend сервер

## 🔒 Безопасность

Платформа использует `RestrictedPython` для безопасного выполнения кода:
- Запрещены файловые операции
- Запрещены сетевые запросы
- Запрещены системные вызовы
- Ограничен набор доступных функций

## 🐛 Решение проблем

### Backend не запускается
- Убедись, что порт 5000 свободен
- Проверь, что все зависимости установлены: `pip list`

### Frontend не подключается к backend
- Убедись, что backend запущен
- Проверь консоль браузера на ошибки CORS
- Убедись, что используешь правильный URL в `app.js`

### Задания не загружаются
- Проверь, что файлы заданий находятся в правильной директории
- Убедись, что JSON файлы валидны (можно проверить на jsonlint.com)

## 📚 Структура проекта

```
code_checker_platform/
├── backend/
│   ├── app.py              # Flask API сервер
│   ├── code_executor.py    # Безопасное выполнение кода
│   └── test_checker.py     # Проверка заданий
├── frontend/
│   ├── index.html          # Главная страница
│   └── static/
│       ├── style.css       # Стили
│       └── app.js          # JavaScript логика
├── exercises/              # Задания в JSON формате
│   └── lesson_03a/
│       ├── exercise_6.json
│       └── exercise_7.json
├── requirements.txt        # Python зависимости
└── README.md              # Документация
```

## 🎯 Следующие шаги

1. Добавь больше заданий для всех уроков
2. Добавь систему сохранения прогресса
3. Добавь авторизацию пользователей
4. Улучши интерфейс (анимации, звуки)
5. Добавь статистику выполнения заданий

