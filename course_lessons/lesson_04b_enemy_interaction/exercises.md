# Упражнения для урока 4B: Взаимодействие с врагами

## 🟢 Простые задания (для закрепления основ)

### Задание 1: Измени количество жизней
**Цель:** Понять систему жизней

**Что сделать:**
- Найди в классе `Player` строку `self.lives = 3`
- Попробуй разные значения:
  - Очень мало: `1` (одна жизнь)
  - Много: `5` или `10`
- Запусти игру и попробуй столкнуться с врагом несколько раз

**Вопрос:** Что произойдёт, если поставить `self.lives = 0` при старте игры?

---

### Задание 2: Измени силу отталкивания при столкновении
**Цель:** Понять механику отталкивания

**Что сделать:**
- Найди строку `knockback = 50` в основном цикле
- Попробуй разные значения:
  - Слабое отталкивание: `20` или `30`
  - Сильное отталкивание: `100` или `150`
- Запусти игру и столкнись с врагом — как далеко отлетает игрок?

**Эксперимент:** Сделай отталкивание очень сильным (200) — что произойдёт? Может ли игрок вылететь за экран?

---

### Задание 3: Измени цвет врага при попадании снаряда
**Цель:** Добавить визуальную обратную связь

**Что сделать:**
- Сейчас враг просто исчезает при попадании
- Попробуй добавить эффект "вспышки" перед исчезновением:
  - Перед удалением врага из списка, нарисуй его белым цветом на один кадр
  - Или нарисуй маленький взрыв (круг) на месте врага

**Подсказка:** Можно добавить список "взрывов" и рисовать их несколько кадров перед удалением.

---

## 🟡 Задания на подумать (для развития логики)

### Задание 4: Враги с несколькими жизнями
**Цель:** Создать более сложную механику боя

**Что сделать:**
- Добавь в класс `Enemy` свойство `health` (здоровье):
  ```python
  def __init__(self, x, y):
      # ... существующий код ...
      self.health = 2  # Враг выдерживает 2 попадания
  ```
- В методе `update` класса `Projectile`, вместо удаления врага сразу:
  ```python
  if self.rect.colliderect(enemy.rect):
      enemy.health -= 1  # Отнимаем одну жизнь
      if enemy.health <= 0:  # Если жизни закончились
          enemies.remove(enemy)  # Удаляем врага
      # ... остальной код рикошета ...
  ```
- В методе `draw` класса `Enemy` рисуй цвет в зависимости от здоровья:
  - Полное здоровье: красный `(200, 0, 0)`
  - Одна жизнь: розовый `(255, 100, 100)`

**Усложнение:** Сделай так, чтобы разные типы врагов имели разное количество жизней (быстрые — 1, обычные — 2, медленные — 3)!

---

### Задание 5: Система очков за убийство врагов
**Цель:** Добавить элемент прогрессии в игру

**Что сделать:**
- Добавь переменную `score = 0` в начале программы
- Когда снаряд попадает во врага и враг удаляется, добавляй очки:
  ```python
  if enemy.health <= 0:
      enemies.remove(enemy)
      score += 10  # Добавляем 10 очков
  ```
- Отображай счёт на экране рядом с жизнями:
  ```python
  score_text = font.render(f"Очки: {score}", True, BLACK)
  screen.blit(score_text, (10, 50))  # Под текстом жизней
  ```

**Усложнение:** 
- Разные враги дают разное количество очков (быстрые — 20, обычные — 10)
- Добавь бонус за убийство нескольких врагов подряд без потери жизни
- Сохраняй лучший результат в файл и показывай его при старте игры!

---

## 💡 Дополнительные идеи для экспериментов

- **Неуязвимость после удара:** Сделай так, чтобы после столкновения с врагом игрок был неуязвим 2 секунды (чтобы не терять все жизни сразу)
- **Визуальная индикация здоровья:** Рисуй полоску здоровья над врагом, если у него больше 1 жизни
- **Враги оставляют след:** Рисуй маленькие точки там, где прошёл враг (след исчезает через несколько секунд)
- **Комбо-система:** Если убиваешь врага снарядом, который только что отскочил от стены, давай больше очков
- **Враги атакуют группами:** Когда враг близко к игроку, он "зовёт" других врагов (ускоряет их движение к игроку)
- **Система паузы:** Добавь клавишу P для паузы игры — все объекты перестают двигаться, но отрисовка продолжается

---

## 📚 Задания на базовые концепции Python (не связанные с проектом)

### Задание 6: Функции с параметрами по умолчанию
**Цель:** Научиться создавать гибкие функции

**Что сделать:**
Создай файл `practice_default_params.py`:

1. **Функция с параметрами по умолчанию:**
   ```python
   def greet(name, greeting="Привет"):
       print(f"{greeting}, {name}!")
   
   greet("Аня")  # Использует "Привет" по умолчанию
   greet("Ваня", "Здравствуй")  # Использует свой привет
   ```

2. **Функция для создания игрока:**
   ```python
   def create_player(name, level=1, health=100, gold=0):
       return {
           "name": name,
           "level": level,
           "health": health,
           "gold": gold
       }
   
   player1 = create_player("Герой")
   player2 = create_player("Воин", level=5, gold=500)
   print(player1)
   print(player2)
   ```

**Усложнение:** Создай функцию `attack`, которая принимает атакующего, цель и урон (по умолчанию 10), и уменьшает здоровье цели!

---

### Задание 7: Работа со словарями и вложенными структурами
**Цель:** Научиться работать со сложными структурами данных

**Что сделать:**
Создай файл `practice_nested_dict.py`:

1. **Создай словарь с вложенными данными:**
   ```python
   game_data = {
       "player": {
           "name": "Герой",
           "level": 5,
           "health": 100,
           "inventory": ["меч", "щит", "зелье"]
       },
       "enemies": [
           {"name": "Гоблин", "health": 50},
           {"name": "Орк", "health": 100}
       ],
       "score": 1500
   }
   ```

2. **Обратись к вложенным данным:**
   ```python
   print(game_data["player"]["name"])
   print(game_data["player"]["inventory"][0])
   print(game_data["enemies"][0]["name"])
   ```

3. **Измени данные:**
   ```python
   game_data["player"]["health"] -= 20
   game_data["player"]["inventory"].append("лук")
   game_data["score"] += 100
   ```

**Усложнение:** Создай функцию `add_enemy`, которая добавляет нового врага в список `enemies`!

---

### Задание 8: Наследование классов
**Цель:** Понять основы наследования в ООП

**Что сделать:**
Создай файл `practice_inheritance.py`:

1. **Базовый класс "Персонаж":**
   ```python
   class Character:
       def __init__(self, name, health):
           self.name = name
           self.health = health
       
       def attack(self):
           print(f"{self.name} атакует!")
       
       def take_damage(self, damage):
           self.health -= damage
           print(f"{self.name} получил {damage} урона. Здоровье: {self.health}")
   ```

2. **Класс "Игрок" наследуется от "Персонаж":**
   ```python
   class Player(Character):
       def __init__(self, name, health, level):
           super().__init__(name, health)  # Вызываем конструктор родителя
           self.level = level
       
       def level_up(self):
           self.level += 1
           self.health += 20
           print(f"{self.name} повысил уровень до {self.level}!")
   ```

3. **Класс "Враг" тоже наследуется:**
   ```python
   class Enemy(Character):
       def __init__(self, name, health, enemy_type):
           super().__init__(name, health)
           self.type = enemy_type
       
       def special_attack(self):
           print(f"{self.name} использует специальную атаку!")
   ```

**Усложнение:** Создай класс "Босс", который наследуется от "Враг" и имеет дополнительное свойство `armor`!

---

### Задание 9: Обработка исключений (try/except)
**Цель:** Научиться обрабатывать ошибки

**Что сделать:**
Создай файл `practice_exceptions.py`:

1. **Обработай деление на ноль:**
   ```python
   def divide(a, b):
       try:
           result = a / b
           return result
       except ZeroDivisionError:
           print("Ошибка: деление на ноль!")
           return None
   
   print(divide(10, 2))  # Работает
   print(divide(10, 0))   # Выведет ошибку
   ```

2. **Обработай неправильный ввод:**
   ```python
   def get_number():
       try:
           number = int(input("Введи число: "))
           return number
       except ValueError:
           print("Это не число! Попробуй ещё раз.")
           return None
   
   num = get_number()
   if num is not None:
       print(f"Ты ввёл: {num}")
   ```

3. **Обработай доступ к несуществующему ключу:**
   ```python
   player = {"name": "Герой", "level": 5}
   
   try:
       print(player["health"])
   except KeyError:
       print("У игрока нет свойства 'health'")
       player["health"] = 100  # Добавляем значение по умолчанию
   ```

**Усложнение:** Создай функцию `safe_get`, которая безопасно получает значение из словаря, возвращая значение по умолчанию, если ключа нет!

---

### Задание 10: Модули и импорты
**Цель:** Научиться организовывать код в модули

**Что сделать:**

1. **Создай файл `game_utils.py` с полезными функциями:**
   ```python
   def calculate_damage(attack, defense):
       damage = max(1, attack - defense)
       return damage
   
   def is_alive(health):
       return health > 0
   
   def format_score(score):
       return f"Очки: {score:,}"  # Добавляет запятые для больших чисел
   ```

2. **Создай файл `main.py` и импортируй функции:**
   ```python
   from game_utils import calculate_damage, is_alive, format_score
   
   # Используй функции
   damage = calculate_damage(50, 20)
   print(f"Урон: {damage}")
   
   if is_alive(75):
       print("Игрок жив!")
   
   print(format_score(1500))
   ```

**Усложнение:** Создай модуль `characters.py` с классами `Player` и `Enemy`, затем импортируй их в основной файл и создай несколько объектов!

