# Упражнения для урока 3B: Физика снарядов (рикошет и подбор)

## 🟢 Простые задания (для закрепления основ)

### Задание 1: Измени максимальное количество снарядов
**Цель:** Понять систему ограничений

**Что сделать:**
- Найди строку `MAX_PROJECTILES = 3`
- Измени значение на 1, 5 или 10
- Запусти игру и попробуй выстрелить несколько раз
- Посмотри, сколько снарядов можно иметь одновременно

**Вопрос:** Что произойдёт, если поставить `MAX_PROJECTILES = 0`? А если `MAX_PROJECTILES = 100`?

---

### Задание 2: Измени расстояние подбора снарядов
**Цель:** Понять, как работает зона подбора

**Что сделать:**
- Найди строку `PICKUP_DISTANCE = 40`
- Измени значение на 20 (ближе) или 80 (дальше)
- Запусти игру, выстрели и подойди к застрявшему снаряду
- Попробуй подобрать его — изменилось ли расстояние?

**Эксперимент:** Сделай расстояние подбора очень большим (200) — что произойдёт?

---

### Задание 3: Измени цвет застрявшего снаряда
**Цель:** Визуально отличать разные состояния объекта

**Что сделать:**
- Найди в методе `draw` класса `Projectile` строку с цветом:
  ```python
  color = (100, 100, 100) if self.stuck else (60, 60, 60)
  ```
- Измени цвет застрявшего снаряда на что-то яркое:
  - Красный: `(255, 0, 0)`
  - Зелёный: `(0, 255, 0)` — чтобы было видно, что его можно подобрать!
  - Жёлтый: `(255, 255, 0)`

**Идея:** Можно сделать так, чтобы застрявший снаряд мигал (менял цвет каждую секунду)!

---

## 🟡 Задания на подумать (для развития логики)

### Задание 4: Рикошет от потолка
**Цель:** Добавить новую физику столкновений

**Что сделать:**
- В методе `update` класса `Projectile` уже есть проверка потолка:
  ```python
  if self.rect.top <= 0:
      self.velocity.y *= -0.4
      self.hit_surface = True
  ```
- Но эта проверка работает только после первого рикошета
- Попробуй переместить эту проверку в блок `if not self.hit_surface:`
- Теперь снаряд будет отскакивать от потолка даже при первом полёте!

**Усложнение:** Сделай так, чтобы снаряд отскакивал от потолка несколько раз, пока не упадёт на землю.

---

### Задание 5: Сила рикошета зависит от угла удара
**Цель:** Создать более реалистичную физику

**Что сделать:**
- Сейчас при рикошете скорость умножается на `-0.4` (теряется 60% скорости)
- Попробуй сделать так, чтобы сила отскока зависела от угла:
  - Если снаряд ударился почти перпендикулярно (угол близок к 90°), отскок сильнее
  - Если снаряд ударился под острым углом, отскок слабее

**Подсказка:** Используй `abs(self.velocity.x)` и `abs(self.velocity.y)` чтобы определить, под каким углом летел снаряд.

**Формула для размышления:**
- Если `abs(velocity.x) > abs(velocity.y)`, значит снаряд летел больше горизонтально
- Если `abs(velocity.y) > abs(velocity.x)`, значит снаряд летел больше вертикально

---

## 💡 Дополнительные идеи для экспериментов

- **Снаряд с таймером:** Сделай так, чтобы снаряд исчезал через 5 секунд после выстрела, даже если не застрял
- **Визуальная индикация:** Нарисуй круг вокруг игрока, показывающий зону подбора снарядов
- **Звуковой эффект:** Добавь печать в консоль (`print("Подобрал снаряд!")`) когда игрок подбирает снаряд
- **Разные типы снарядов:** Сделай так, чтобы при клике правой кнопкой мыши создавался "быстрый" снаряд (скорость 20), а при левой — обычный

---

## 📚 Задания на базовые концепции Python (не связанные с проектом)

### Задание 6: Класс с несколькими методами
**Цель:** Углубить понимание классов и методов

**Что сделать:**
Создай файл `practice_class_methods.py`:

1. **Создай класс "Счётчик":**
   ```python
   class Counter:
       def __init__(self):
           self.count = 0
       
       def increment(self):
           self.count += 1
       
       def decrement(self):
           self.count -= 1
       
       def reset(self):
           self.count = 0
       
       def get_value(self):
           return self.count
   ```

2. **Используй счётчик:**
   ```python
   counter = Counter()
   counter.increment()
   counter.increment()
   print(counter.get_value())  # Должно быть 2
   counter.decrement()
   print(counter.get_value())  # Должно быть 1
   counter.reset()
   print(counter.get_value())  # Должно быть 0
   ```

**Усложнение:** Создай класс "Таймер", который считает секунды и может быть запущен, остановлен и сброшен!

---

### Задание 7: Работа со списками объектов
**Цель:** Научиться хранить и обрабатывать несколько объектов

**Что сделать:**
Создай файл `practice_list_objects.py`:

1. **Создай класс "Игрок":**
   ```python
   class Player:
       def __init__(self, name, score):
           self.name = name
           self.score = score
       
       def __str__(self):
           return f"{self.name}: {self.score} очков"
   ```

2. **Создай список игроков:**
   ```python
   players = [
       Player("Аня", 100),
       Player("Ваня", 150),
       Player("Маша", 80),
       Player("Петя", 200)
   ]
   
   # Выведи всех игроков
   for player in players:
       print(player)
   ```

3. **Найди игрока с наибольшим количеством очков:**
   ```python
   best_player = players[0]
   for player in players:
       if player.score > best_player.score:
           best_player = player
   
   print(f"Лучший игрок: {best_player}")
   ```

**Усложнение:** Отсортируй список игроков по очкам (от большего к меньшему)!

---

### Задание 8: Условные операторы и логика
**Цель:** Разобраться с логическими операторами

**Что сделать:**
Создай файл `practice_logic.py`:

1. **Проверь несколько условий:**
   ```python
   age = 12
   has_permission = True
   money = 50
   
   # Используй and (и)
   if age >= 10 and has_permission:
       print("Можешь играть")
   
   # Используй or (или)
   if age < 8 or money < 20:
       print("Нужна помощь")
   
   # Используй not (не)
   if not has_permission:
       print("Нет разрешения")
   ```

2. **Сложные условия:**
   ```python
   score = 85
   level = 5
   
   if score >= 90 and level >= 10:
       print("Отличный результат!")
   elif score >= 70 or level >= 5:
       print("Хороший результат")
   else:
       print("Продолжай тренироваться")
   ```

**Усложнение:** Создай систему проверки доступа: игрок может играть, если (возраст >= 10 И есть разрешение) ИЛИ (возраст >= 16)!

---

### Задание 9: Цикл while
**Цель:** Научиться использовать цикл while для повторения действий

**Что сделать:**
Создай файл `practice_while.py`:

1. **Счётчик до определённого числа:**
   ```python
   count = 0
   while count < 5:
       print(f"Счёт: {count}")
       count += 1
   ```

2. **Игра "Угадай число":**
   ```python
   secret_number = 7
   guess = 0
   
   while guess != secret_number:
       guess = int(input("Угадай число от 1 до 10: "))
       if guess < secret_number:
           print("Слишком мало!")
       elif guess > secret_number:
           print("Слишком много!")
   
   print("Угадал!")
   ```

3. **Сумма чисел:**
   ```python
   total = 0
   number = 1
   
   while number <= 10:
       total += number
       number += 1
   
   print(f"Сумма чисел от 1 до 10: {total}")
   ```

**Усложнение:** Создай программу, которая просит вводить числа до тех пор, пока не будет введено 0, затем выводит сумму всех введённых чисел!

---

### Задание 10: Словари (dictionaries)
**Цель:** Научиться работать со словарями для хранения пар ключ-значение

**Что сделать:**
Создай файл `practice_dict.py`:

1. **Создай словарь игрока:**
   ```python
   player = {
       "name": "Герой",
       "level": 5,
       "health": 100,
       "gold": 500,
       "items": ["меч", "щит", "зелье"]
   }
   
   # Получи значение по ключу
   print(player["name"])
   print(player["level"])
   
   # Измени значение
   player["health"] = 80
   player["gold"] += 100
   
   # Добавь новый ключ
   player["experience"] = 250
   ```

2. **Перебери словарь:**
   ```python
   for key, value in player.items():
       print(f"{key}: {value}")
   ```

3. **Проверь наличие ключа:**
   ```python
   if "health" in player:
       print(f"Здоровье: {player['health']}")
   
   if "mana" not in player:
       print("У игрока нет маны")
   ```

**Усложнение:** Создай словарь с информацией о нескольких игроках, где ключ — имя игрока, а значение — словарь с его характеристиками!

