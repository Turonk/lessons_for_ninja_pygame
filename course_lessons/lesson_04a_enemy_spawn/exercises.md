# Урок 4A: Спавн врагов - Практические задания

## Задание 1: Переменные и операции 🐱
**Тема:** Создание переменных, простые операции, вывод в консоль

Создайте программу, которая описывает кота-ниндзя. Вам нужно:

1. Создать **4 переменные** для характеристик кота:
   - `name` (строка) - имя кота
   - `age` (целое число) - возраст кота
   - `speed` (целое число) - скорость кота
   - `health` (целое число) - здоровье кота

2. Выполнить **простые операции**:
   - Увеличить скорость кота на 5 (сложение)
   - Уменьшить здоровье кота в 2 раза (деление)
   - Вычислить "силу" кота (умножение возраста на скорость)

3. **Вывести все результаты** в консоль с понятными сообщениями

**Пример вывода:**
```
Имя кота: Ниндзя
Возраст: 12
Скорость после тренировки: 15
Здоровье после боя: 25
Сила кота: 180
```

**Подсказка:** Используйте функции `print()` для вывода результатов.

---

## Задание 2: Цикл for для перебора списка 🔄
**Тема:** Работа со списками, цикл for

Создайте программу, которая показывает разных врагов кота-ниндзя:

1. Создайте **список врагов** с 5 разными именами:
   ```python
   enemies = ["Волк", "Лиса", "Собака", "Крыса", "Змея"]
   ```

2. Используйте **цикл for** для перебора списка:
   - Для каждого врага вывести его имя
   - Добавить счетчик (номер по порядку)
   - Вывести предупреждение типа "Осторожно! Враг №1: Волк!"

3. **Дополнительно:** Создайте список скоростей для каждого врага и выведите их вместе

**Пример вывода:**
```
Осторожно! Враг №1: Волк - скорость 8
Осторожно! Враг №2: Лиса - скорость 6
Осторожно! Враг №3: Собака - скорость 7
...
```

**Подсказка:** Используйте `enumerate()` для получения номера элемента или простой счетчик.

---

## Задание 3: Условный оператор (if/else) 🤔
**Тема:** Проверка условий, принятие решений

Создайте программу, которая проверяет готовность кота-ниндзя к бою:

1. Создайте **переменные** для состояния кота:
   - `health` (здоровье) - число от 0 до 100
   - `speed` (скорость) - число от 0 до 20
   - `has_weapon` (есть ли оружие) - True или False

2. Используйте **условные операторы if/else** для проверки:
   - Если здоровье > 50 И скорость > 10 И есть оружие → "Кот готов к бою! 🐱⚔️"
   - Если здоровье < 30 → "Кот ранен! Нужно лечение! 🩹"
   - Если скорость < 5 → "Кот слишком медленный! Нужно тренировка! 🏃"
   - Если нет оружия → "Кот безоружен! Найди меч! ⚔️"

3. **Протестируйте** разные комбинации значений

**Пример кода:**
```python
health = 75
speed = 15
has_weapon = True

if health > 50 and speed > 10 and has_weapon:
    print("Кот готов к бою! 🐱⚔️")
elif health < 30:
    print("Кот ранен! Нужно лечение! 🩹")
# ... остальные условия
```

**Подсказка:** Используйте операторы `and`, `or` для сложных условий.

---

## Задание 4: Функции с return 📦
**Тема:** Создание функций, возвращение значений

Создайте программу с функциями для боевой системы кота-ниндзя:

1. Создайте **функцию `calculate_damage()`** которая:
   - Принимает параметры: сила атаки и тип оружия
   - Возвращает рассчитанный урон
   - Логика: урон = сила_атаки * множитель_оружия

2. Создайте **функцию `is_alive()`** которая:
   - Принимает здоровье кота
   - Возвращает True если здоровье > 0, иначе False

3. Создайте **функцию `generate_enemy_speed()`** которая:
   - Принимает тип врага
   - Возвращает случайную скорость для этого типа

4. **Используйте функции** в основной программе:
   - Создайте бой между котом и врагом
   - Используйте функции для расчетов
   - Выведите результаты боя

**Пример кода:**
```python
import random

def calculate_damage(attack_power, weapon_type):
    if weapon_type == "меч":
        multiplier = 2
    elif weapon_type == "когти":
        multiplier = 1.5
    else:
        multiplier = 1
    return int(attack_power * multiplier)

def is_alive(health):
    return health > 0

# Основная программа
cat_damage = calculate_damage(10, "меч")
print(f"Урон кота: {cat_damage}")

cat_health = 50
print(f"Кот жив: {is_alive(cat_health)}")
```

**Подсказка:** Функции должны возвращать значения с помощью `return`, а не печатать их.

---

## 🎯 Проверочные вопросы:

1. **Переменные:** Какие типы данных вы использовали? (строки, числа)
2. **Циклы:** Как работает цикл for со списком?
3. **Условия:** Когда используется elif? В чем разница между and/or?
4. **Функции:** Зачем нужно return? Чем отличается print() от return?

## 🏆 Дополнительное задание:

Создайте мини-игру, где кот-ниндзя сражается с врагами:
- Используйте все изученные концепции
- Кот может прыгать и атаковать
- Враги появляются со случайной скоростью
- Побеждает тот, у кого осталось здоровье

**Удачи в выполнении заданий!** 🐱‍👤