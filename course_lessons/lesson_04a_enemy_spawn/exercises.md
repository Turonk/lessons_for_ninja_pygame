# Упражнения для урока 4A: Враги - ходьба и спавн по таймеру

## 🟢 Простые задания (для закрепления основ)

### Задание 1: Измени скорость врагов
**Цель:** Понять, как работает движение врагов

**Что сделать:**
- Найди строку `ENEMY_SPEED = 2`
- Попробуй разные значения:
  - Очень медленные: `0.5` или `1`
  - Быстрые: `5` или `10`
- Запусти игру и посмотри, как изменилось поведение врагов

**Вопрос:** Что произойдёт, если поставить скорость `0`? А если `-2` (отрицательная)?

---

### Задание 2: Измени частоту появления врагов
**Цель:** Понять работу таймера

**Что сделать:**
- Найди строку `SPAWN_DELAY = 180`
- Помни: 180 кадров = 3 секунды (при 60 кадрах в секунду)
- Попробуй разные значения:
  - Чаще: `60` (1 секунда) или `30` (0.5 секунды)
  - Реже: `360` (6 секунд) или `600` (10 секунд)
- Запусти игру и посчитай, как часто появляются враги

**Подсказка:** Чтобы перевести секунды в кадры, умножь на 60. Например, 5 секунд = 5 × 60 = 300 кадров.

---

### Задание 3: Измени размер и цвет врагов
**Цель:** Научиться кастомизировать внешний вид

**Что сделать:**
- Найди строку `ENEMY_SIZE = 40` и измени размер (например, на 30 или 60)
- Найди в методе `draw` класса `Enemy` цвет `(200, 0, 0)` (красный)
- Попробуй разные цвета:
  - Синий: `(0, 0, 200)`
  - Зелёный: `(0, 200, 0)`
  - Фиолетовый: `(200, 0, 200)`
  - Оранжевый: `(255, 165, 0)`

**Эксперимент:** Сделай врагов очень маленькими (20) и очень большими (80). Как это влияет на игру?

---

## 🟡 Задания на подумать (для развития логики)

### Задание 4: Враги появляются только с одной стороны
**Цель:** Научиться контролировать спавн

**Что сделать:**
- Найди место, где выбирается сторона появления:
  ```python
  side = random.choice(["left", "right"])
  ```
- Вместо случайного выбора, сделай так, чтобы враги всегда появлялись слева:
  ```python
  side = "left"
  ```
- Или всегда справа:
  ```python
  side = "right"
  ```

**Усложнение:** Сделай так, чтобы первые 3 врага появлялись слева, следующие 3 справа, и так далее по очереди!

**Подсказка:** Используй счётчик: `spawn_count = 0`, и проверяй `if spawn_count % 6 < 3:` (первые 3) или `else:` (следующие 3).

---

### Задание 5: Два типа врагов: быстрые и медленные
**Цель:** Создать разнообразие в игре

**Что сделать:**
- Добавь в класс `Enemy` параметр `enemy_type`:
  ```python
  def __init__(self, x, y, enemy_type="normal"):
      self.type = enemy_type
      if enemy_type == "fast":
          self.speed = 4  # Быстрый враг
      else:
          self.speed = 2  # Обычный враг
  ```
- В методе `update` используй `self.speed` вместо `ENEMY_SPEED`
- При спавне случайно выбирай тип:
  ```python
  enemy_type = random.choice(["normal", "fast"])
  enemy = Enemy(x, y, enemy_type)
  ```
- В методе `draw` рисуй быстрых врагов другим цветом (например, оранжевым)

**Усложнение:** Добавь третий тип — "медленный" враг (скорость 1), который рисуется синим цветом!

---

## 💡 Дополнительные идеи для экспериментов

- **Враги с разными размерами:** Сделай так, чтобы размер врага выбирался случайно от 30 до 50 пикселей
- **Визуальная индикация:** Показывай на экране, сколько врагов появилось всего (не только сейчас на экране)
- **Враги появляются группами:** Вместо одного врага каждые 3 секунды, появляй сразу 3 врага каждые 10 секунд
- **Враги двигаются зигзагом:** Добавь вертикальное движение (вверх-вниз) при движении к игроку
- **Ограничение количества врагов:** Сделай так, чтобы одновременно на экране было не больше 5 врагов

---

## 📚 Задания на базовые концепции Python (не связанные с проектом)

### Задание 6: Циклы и списки вместе
**Цель:** Научиться обрабатывать списки с помощью циклов

**Что сделать:**
Создай файл `practice_loops_lists.py`:

1. **Создай список и обработай его:**
   ```python
   numbers = [5, 10, 15, 20, 25]
   
   # Выведи все числа
   for num in numbers:
       print(num)
   
   # Выведи числа с индексами
   for i in range(len(numbers)):
       print(f"Индекс {i}: {numbers[i]}")
   ```

2. **Найди сумму и среднее:**
   ```python
   total = 0
   for num in numbers:
       total += num
   
   average = total / len(numbers)
   print(f"Сумма: {total}")
   print(f"Среднее: {average}")
   ```

3. **Создай новый список на основе старого:**
   ```python
   # Удвой все числа
   doubled = []
   for num in numbers:
       doubled.append(num * 2)
   print(doubled)
   
   # Или короче (списковое включение):
   doubled = [num * 2 for num in numbers]
   ```

**Усложнение:** Создай список из 10 случайных чисел от 1 до 100, затем найди максимальное и минимальное значение!

---

### Задание 7: Работа с несколькими списками
**Цель:** Научиться синхронизировать данные в разных списках

**Что сделать:**
Создай файл `practice_multiple_lists.py`:

1. **Создай связанные списки:**
   ```python
   names = ["Аня", "Ваня", "Маша"]
   scores = [100, 150, 80]
   levels = [5, 7, 3]
   
   # Выведи информацию о каждом игроке
   for i in range(len(names)):
       print(f"{names[i]}: {scores[i]} очков, уровень {levels[i]}")
   ```

2. **Найди игрока с лучшим результатом:**
   ```python
   best_index = 0
   for i in range(len(scores)):
       if scores[i] > scores[best_index]:
           best_index = i
   
   print(f"Лучший игрок: {names[best_index]} с {scores[best_index]} очками")
   ```

3. **Добавь нового игрока:**
   ```python
   names.append("Петя")
   scores.append(200)
   levels.append(10)
   ```

**Усложнение:** Используй словарь вместо нескольких списков! Создай список словарей, где каждый словарь содержит информацию об одном игроке.

---

### Задание 8: Функции
**Цель:** Научиться создавать и использовать функции

**Что сделать:**
Создай файл `practice_functions.py`:

1. **Простая функция:**
   ```python
   def greet(name):
       print(f"Привет, {name}!")
   
   greet("Аня")
   greet("Ваня")
   ```

2. **Функция с возвращаемым значением:**
   ```python
   def add_numbers(a, b):
       return a + b
   
   result = add_numbers(5, 3)
   print(result)  # Выведет 8
   ```

3. **Функция с несколькими параметрами:**
   ```python
   def calculate_score(base_score, bonus, multiplier):
       total = (base_score + bonus) * multiplier
       return total
   
   score = calculate_score(100, 50, 1.5)
   print(f"Итоговый счёт: {score}")
   ```

**Усложнение:** Создай функцию `find_max`, которая принимает список чисел и возвращает максимальное значение!

---

### Задание 9: Класс с состоянием
**Цель:** Понять, как объекты хранят состояние

**Что сделать:**
Создай файл `practice_state.py`:

1. **Создай класс "Банковский счёт":**
   ```python
   class BankAccount:
       def __init__(self, owner, balance=0):
           self.owner = owner
           self.balance = balance
       
       def deposit(self, amount):
           self.balance += amount
           print(f"Пополнено {amount}. Баланс: {self.balance}")
       
       def withdraw(self, amount):
           if amount <= self.balance:
               self.balance -= amount
               print(f"Снято {amount}. Баланс: {self.balance}")
           else:
               print("Недостаточно средств!")
       
       def get_balance(self):
           return self.balance
   ```

2. **Используй счёт:**
   ```python
   account = BankAccount("Аня", 1000)
   account.deposit(500)
   account.withdraw(200)
   account.withdraw(2000)  # Должно вывести ошибку
   print(f"Текущий баланс: {account.get_balance()}")
   ```

**Усложнение:** Добавь метод `transfer`, который переводит деньги с одного счёта на другой!

---

### Задание 10: Генерация случайных чисел
**Цель:** Научиться использовать модуль random

**Что сделать:**
Создай файл `practice_random.py`:

1. **Импортируй модуль random:**
   ```python
   import random
   ```

2. **Генерируй случайные числа:**
   ```python
   # Случайное число от 1 до 10
   number = random.randint(1, 10)
   print(number)
   
   # Случайное число от 0 до 100
   score = random.randint(0, 100)
   print(f"Случайный счёт: {score}")
   ```

3. **Выбирай случайный элемент из списка:**
   ```python
   colors = ["красный", "синий", "зелёный", "жёлтый"]
   chosen_color = random.choice(colors)
   print(f"Выбранный цвет: {chosen_color}")
   ```

4. **Перемешай список:**
   ```python
   numbers = [1, 2, 3, 4, 5]
   random.shuffle(numbers)
   print(numbers)
   ```

**Усложнение:** Создай игру "Угадай число", где компьютер загадывает число от 1 до 100, а игрок пытается угадать!

