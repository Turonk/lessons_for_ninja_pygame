# Упражнения для урока 3A: Выстрел в сторону мышки

## 🟢 Простые задания (для закрепления основ)

### Задание 1: Измени скорость снаряда
**Цель:** Понять, как работает константа `PROJECTILE_SPEED`

**Что сделать:**
- Найди строку с `PROJECTILE_SPEED = 10`
- Попробуй изменить значение на 5, 15, 20
- Запусти игру и посмотри, как изменилась скорость полёта снаряда

**Вопрос для размышления:** Что происходит, когда скорость очень большая (например, 50)? А когда очень маленькая (например, 2)?

---

### Задание 2: Измени размер снаряда
**Цель:** Научиться изменять размер объектов

**Что сделать:**
- Найди в классе `Projectile` строку `self.rect = pygame.Rect(x, y, 12, 12)`
- Числа 12, 12 — это ширина и высота снаряда
- Измени их на 20, 20 (больше) или 8, 8 (меньше)
- Запусти игру и посмотри на результат

**Подсказка:** Можно сделать снаряд прямоугольным! Попробуй 20, 8 (широкий) или 8, 20 (высокий)

---

### Задание 3: Измени цвет снаряда
**Цель:** Понять, как работают цвета в Pygame

**Что сделать:**
- Найди в методе `draw` класса `Projectile` строку с цветом: `(60, 60, 60)` — это серый цвет
- Цвет задаётся тремя числами от 0 до 255: (красный, зелёный, синий)
- Попробуй разные цвета:
  - Красный: `(255, 0, 0)`
  - Синий: `(0, 0, 255)`
  - Жёлтый: `(255, 255, 0)`
  - Зелёный: `(0, 255, 0)`

**Эксперимент:** Попробуй сделать снаряд ярко-оранжевым! Какие числа нужно использовать?

---

## 🟡 Задания на подумать (для развития логики)

### Задание 4: Снаряд летит в противоположную сторону
**Цель:** Понять, как работает направление вектора

**Что сделать:**
- Найди в классе `Projectile` строки, где вычисляется направление:
  ```python
  direction = pygame.math.Vector2(target_pos[0] - x, target_pos[1] - y)
  ```
- Чтобы снаряд летел в противоположную сторону, нужно поменять знаки:
  ```python
  direction = pygame.math.Vector2(x - target_pos[0], y - target_pos[1])
  ```
- Запусти игру и попробуй выстрелить — снаряд полетит в противоположную сторону от мышки!

**Вопрос:** Почему это работает? Что изменилось в формуле?

---

### Задание 5: Несколько снарядов одновременно
**Цель:** Научиться создавать несколько объектов сразу

**Что сделать:**
- Найди место, где создаётся снаряд при клике мышки
- Вместо одного снаряда создай три снаряда, которые летят в разные стороны:
  - Один в сторону мышки (как обычно)
  - Один немного левее (отними 30 от X координаты мышки)
  - Один немного правее (прибавь 30 к X координате мышки)

**Подсказка:** Используй цикл `for` или создай три снаряда подряд с разными координатами цели.

**Усложнение:** Сделай так, чтобы снаряды разлетались веером (один вверх, один прямо, один вниз)!

---

## 💡 Дополнительные идеи для экспериментов

- Попробуй сделать снаряд круглым вместо квадрата (используй `pygame.draw.circle`)
- Добавь след за снарядом (рисуй маленькие точки там, где снаряд был раньше)
- Сделай так, чтобы снаряд исчезал не сразу при выходе за экран, а через 2 секунды после выстрела

---

## 📚 Задания на базовые концепции Python (не связанные с проектом)

### Задание 6: Работа с переменными
**Цель:** Закрепить понимание переменных и типов данных

**Что сделать:**
Создай новый файл `practice_variables.py` и выполни следующие задания:

1. **Создай переменные разных типов:**
   ```python
   # Числа
   age = 12
   height = 150.5
   
   # Строки
   name = "Игрок"
   
   # Список
   colors = ["красный", "синий", "зелёный"]
   
   # Логическое значение (True/False)
   is_active = True
   ```

2. **Выведи все переменные на экран:**
   ```python
   print(f"Имя: {name}, Возраст: {age}, Рост: {height}")
   print(f"Цвета: {colors}")
   print(f"Активен: {is_active}")
   ```

3. **Измени значения переменных:**
   - Увеличь возраст на 1
   - Добавь новый цвет в список
   - Измени `is_active` на противоположное значение

**Вопрос:** Какие типы данных ты использовал? Чем они отличаются?

---

### Задание 7: Цикл for
**Цель:** Научиться использовать циклы для повторения действий

**Что сделать:**
Создай файл `practice_loops.py`:

1. **Выведи числа от 1 до 10:**
   ```python
   for i in range(1, 11):
       print(i)
   ```

2. **Выведи все элементы списка:**
   ```python
   fruits = ["яблоко", "банан", "апельсин", "груша"]
   for fruit in fruits:
       print(f"Фрукт: {fruit}")
   ```

3. **Выведи числа с шагом:**
   - От 0 до 20 с шагом 2 (чётные числа)
   - От 10 до 0 (обратный порядок)

**Усложнение:** Создай список из 5 имён друзей и выведи их с номерами (1. Имя1, 2. Имя2...)

---

### Задание 8: Условия if/else
**Цель:** Научиться принимать решения в программе

**Что сделать:**
Создай файл `practice_conditions.py`:

1. **Проверь возраст:**
   ```python
   age = 12
   if age >= 18:
       print("Ты взрослый")
   elif age >= 13:
       print("Ты подросток")
   else:
       print("Ты ребёнок")
   ```

2. **Проверь число:**
   ```python
   number = 15
   if number > 10:
       print("Число больше 10")
   if number % 2 == 0:
       print("Число чётное")
   else:
       print("Число нечётное")
   ```

3. **Проверь список:**
   ```python
   items = ["меч", "щит", "золото"]
   if "меч" in items:
       print("У тебя есть меч!")
   if len(items) > 5:
       print("У тебя много предметов")
   else:
       print("Предметов немного")
   ```

**Усложнение:** Создай программу-викторину с вопросами и проверкой ответов!

---

### Задание 9: Простой класс
**Цель:** Понять основы объектно-ориентированного программирования

**Что сделать:**
Создай файл `practice_class.py`:

1. **Создай класс "Кот":**
   ```python
   class Cat:
       def __init__(self, name, age):
           self.name = name
           self.age = age
           self.hungry = True
       
       def feed(self):
           self.hungry = False
           print(f"{self.name} покормлен!")
       
       def meow(self):
           print(f"{self.name} говорит: Мяу!")
   ```

2. **Создай несколько котов:**
   ```python
   cat1 = Cat("Мурзик", 3)
   cat2 = Cat("Барсик", 5)
   
   cat1.meow()
   cat2.feed()
   ```

3. **Добавь метод `info`:**
   ```python
   def info(self):
       status = "голодный" if self.hungry else "сытый"
       print(f"{self.name}, {self.age} лет, {status}")
   ```

**Усложнение:** Создай класс "Собака" с методами `bark()` и `play()`, затем создай список из 3 собак и выведи информацию о каждой!

---

### Задание 10: Работа со списками
**Цель:** Научиться добавлять, удалять и изменять элементы списка

**Что сделать:**
Создай файл `practice_lists.py`:

1. **Создай список и работай с ним:**
   ```python
   scores = [10, 20, 15, 30, 25]
   
   # Добавь новый элемент
   scores.append(35)
   
   # Удали элемент
   scores.remove(15)
   
   # Измени элемент
   scores[0] = 50
   
   # Выведи список
   print(scores)
   ```

2. **Найди максимальное и минимальное значение:**
   ```python
   print(f"Максимум: {max(scores)}")
   print(f"Минимум: {min(scores)}")
   print(f"Сумма: {sum(scores)}")
   ```

3. **Отсортируй список:**
   ```python
   scores.sort()  # По возрастанию
   print(scores)
   
   scores.sort(reverse=True)  # По убыванию
   print(scores)
   ```

**Усложнение:** Создай список имён игроков и их очков, затем найди игрока с наибольшим количеством очков!

